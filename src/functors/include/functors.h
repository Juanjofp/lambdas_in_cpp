#pragma once

// Functors are objects that can be called as if they were functions.
// In general, functors are used to encapsulate a function that can be passed as
// an argument to other functions. Functors are also called function objects.

namespace jjfp::functors {
static const char* TAG = "FUNCTORS";

void run_sample();
}  // namespace jjfp::functors